# Workflow 基础

## 文章导读

从今天开始进入 Week 4，主题是 Workflow、Agent 与 Tool Calling。前三个星期我们分别建立了 AI 全景认知、掌握了 API 调用和结构化输出、完成了 RAG 知识库系统的搭建。这一周要把这些能力串起来，让 AI 从"被动的问答工具"变成"能按流程执行任务的系统"。

这篇文章的核心目标是理解 Workflow 的本质：什么是工作流、什么时候该用工作流而不是 Agent、怎么设计节点和状态传递、怎么处理异常和人工介入。读完这一部分，你应该能画出一张清晰的行业分析 Workflow 流程图，并且说出每一个节点做什么、输入什么、输出什么、失败了怎么办。

---

## 核心概念

### Workflow 的定义

Workflow，中文叫工作流，本质上就是"把一件复杂任务拆成一系列有序的步骤，每个步骤有明确的输入和输出，步骤之间按固定规则流转"。

想想工厂流水线。原材料进来，第一道工序切割，第二道工序打磨，第三道工序组装，第四道工序质检，最后包装出库。每道工序只做一件事，做完把半成品传给下一道。如果你把整条流水线看成一个整体，它就是一个 Workflow。

在 AI 应用里，Workflow 就是把"让 AI 做一件复杂的事"拆解成多个步骤，每个步骤可能调用一次大模型、可能查一次数据库、可能调一个外部 API，步骤之间按预定义的顺序执行。

Workflow 有三个核心特征。第一，流程是预定义的，不是动态决定的。你设计好了 6 个节点，它就一定走这 6 个节点，不会临时加一个节点或者跳过某个节点（除非条件分支允许）。第二，每个节点有明确的输入和输出格式。节点 A 的输出格式必须和节点 B 的输入格式匹配，否则链条就断了。第三，状态在节点之间传递。前一个节点的结果作为后一个节点的输入，整个链条共享一个状态上下文。

为什么 Workflow 这么重要？因为企业应用最怕的不是 AI 不够聪明，而是 AI 的行为不可预测。一个固定的流程能保证每次执行的结果在可控范围内，出了问题能定位到具体节点。这是企业客户愿意买单的前提。

### Workflow 适用场景

不是所有场景都适合 Workflow。适合的场景有三个特征。

第一个特征是流程相对固定。比如"行业分析报告生成"，不管分析哪个行业，步骤都差不多：理解需求、收集信息、拆解行业、分析岗位、分析流程、生成报告。这种流程你可以提前设计好，每次按流程走就行。

第二个特征是步骤之间有明确的依赖关系。后一步必须等前一步完成才能开始，而且前一步的输出是后一步的输入。比如"岗位拆解"必须在"行业概览"之后，因为你需要先知道这个行业有哪些业务板块，才能拆出对应的关键岗位。

第三个特征是输出格式要求高。每一步都要产出结构化的结果，而不是一段自由文本。比如"AI 机会评分"这一步，必须输出一张打分表，包含场景名称、评分维度、分数、推荐优先级。这种结构化的中间产物是 Workflow 可靠运行的保障。

反过来说，哪些场景不适合 Workflow？如果任务需要 AI 自主判断下一步做什么、需要根据情况动态调整策略、需要跟外部环境反复交互，那就应该用 Agent 而不是 Workflow。我们在明天的内容里详细对比。

举个实际的例子。假设你给一家半导体企业做 AI 提效方案。你需要先了解这家企业的业务范围、组织架构、核心流程、痛点，然后才能提出方案。这个"诊断"过程本身就是一个 Workflow：企业概况收集 → 业务流程梳理 → 岗位任务拆解 → AI 机会识别 → 方案优先级排序 → 报告生成。每一步都有明确的产出，前后依赖关系清晰。

### 节点设计

节点是 Workflow 的基本组成单元。每个节点代表一个独立的处理步骤。

设计节点时，要回答五个问题：这个节点做什么？输入是什么？输出是什么？失败了怎么办？需要多长时间？

拿"行业概览"这个节点举例。它做的事情是：给定一个行业名称，生成该行业的结构化概览，包括产业链结构、主要玩家、市场规模、发展趋势、核心技术。输入是一个字符串（行业名称）。输出是一个 JSON 对象，包含上述字段。失败的情况包括：行业名称模糊导致结果不准确、模型输出格式错误、模型调用超时。处理时间大约 10-20 秒。

节点设计有一个容易忽视的原则：粒度要适中。太粗了，一个节点做了三件事，出问题不好定位；太细了，节点太多，状态传递复杂度爆炸，维护成本高。一个好的经验是"一个节点做一件逻辑完整的事"。所谓逻辑完整，就是中间产物有自己的独立价值。比如"行业概览"就是一个逻辑完整的节点，它的输出可以单独拿出来看、单独存档、单独复用。

还有一种常见的做法是把节点分为两类：AI 节点和非 AI 节点。AI 节点调用大模型做生成或分析，非 AI 节点做数据处理、格式转换、条件判断、结果存储。一个健康的 Workflow 应该是两类节点混合的，不是所有节点都调模型。

### 状态传递

节点之间需要传递状态。状态是整个 Workflow 的共享上下文，包含了从开始到当前所有已执行节点的输出。

状态传递有两种方式。一种是显式传递，每个节点的输出明确地作为下一个节点的输入。比如节点 A 输出行业概览 JSON，节点 B 接收这个 JSON 作为输入。这种方式清晰但不够灵活——如果节点 C 需要节点 A 和节点 B 的输出，就得把状态合并后传给 C。

另一种是隐式传递，维护一个全局状态对象，每个节点可以从中读取需要的数据，也可以写入自己的输出。比如全局状态包含 {"industry": "半导体", "overview": {...}, "roles": [...], "processes": [...]}。节点 A 写入 overview，节点 B 写入 roles，节点 C 可以同时读取 overview 和 roles。这种方式灵活但可能造成状态膨胀——随着节点增加，全局状态越来越大，后面的节点要处理的数据越来越多。

实践中通常是两种方式结合。关键数据（如行业概览）显式传递，共享数据（如行业名称、配置参数）通过全局状态。

状态膨胀是一个实际问题。当 Workflow 有 10 个节点，每个节点的输出都累积到全局状态中，最后的状态可能有几十 KB。这不仅增加 Token 消耗，也会影响模型的注意力（上下文太长，模型难以聚焦）。解决方法包括：只传递必要的数据、在中间节点做数据压缩、定期清理不再需要的状态字段。

### 条件分支

现实中的 Workflow 很少是纯线性的。经常需要根据中间结果做判断，走不同的路径。

条件分支有两种类型。一种是基于数据值的分支。比如"如果行业概览中的市场规模大于 1000 亿，执行详细分析分支；否则执行简化分析分支"。另一种是基于执行结果的分支。比如"如果节点 A 成功，继续执行节点 B；如果节点 A 失败，跳转到错误处理节点"。

设计条件分支时要避免"节点爆炸"。不要为每种可能情况都设计一个专门的节点，那样 Workflow 会变得复杂难维护。更好的做法是把通用逻辑抽取为公共节点，用参数控制行为。

### 异常处理

Workflow 执行过程中任何节点都可能失败。模型可能超时、API 可能报错、输出格式可能不对。如果每个节点都没有异常处理，整个链条会在第一个失败的节点处中断。

异常处理有三个层次。节点级异常处理在节点内部处理，不中断 Workflow。比如"行业概览"节点调用模型失败，可以重试 3 次，如果还失败就用缓存的历史数据作为降级方案。Workflow 级异常处理在 Workflow 框架层面处理，比如记录日志、发送告警、暂停执行等待人工介入。系统级异常处理在最外层，比如整个 Workflow 失败后的回滚策略、补偿机制。

设计异常处理时要考虑可恢复性。哪些错误是暂时的（网络超时、速率限制），可以通过重试恢复？哪些错误是永久的（参数错误、权限不足），需要人工介入？重试策略是什么？指数退避？固定间隔？最多重试几次？

### 人工介入

有些节点可能需要人工审核或确认。比如"AI 方案推荐"节点生成方案后，需要专家审核才能继续。这种节点不能自动执行，需要暂停 Workflow，等待人工输入。

人工介入节点的设计要明确：审核什么？拒绝标准是什么？谁来审核？审核期限是多久？如果超时未审核怎么办？

审核表单要简洁。不要让人工审核者面对一堆原始输出。应该把关键信息提炼出来，用清晰的结构呈现，审核者只需要做确认或修改。

## 核心结论

Workflow 是把复杂任务拆成有序步骤的系统，每个步骤有明确的输入输出。

适用场景：流程相对固定、步骤间有依赖关系、输出格式要求高。

节点设计要回答五个问题：做什么、输入什么、输出什么、失败了怎么办、需要多长时间。

状态传递有显式和隐式两种，实践中通常是结合使用。要避免状态膨胀。

条件分支基于数据值或执行结果，要避免节点爆炸。

异常处理分节点级、Workflow 级、系统级三个层次。

人工介入节点需要明确审核内容、标准、责任人、期限。

## 相关延伸

上文讨论了 Workflow——预定义流程的系统。Workflow 的优势是可控、可预测，劣势是不够灵活。下一篇会进入 Agent——能自主决策下一步做什么的系统。Agent 和 Workflow 是互补关系，不是替代关系。
